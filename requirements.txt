# Core decoding needs only numpy (vectorised vertex scan in mesh_decode.py).
numpy
# Texture extraction (DDS -> PNG) needs Pillow.
Pillow
# Viewer servers: app.py falls back to stdlib without Flask, but the outfit
# composer (outfit_app.py) requires it.
flask
# Optional: headless visual verification (screenshot.py).
# playwright  (then: playwright install chromium)
